import { storyblokEditable } from '@storyblok/react';

const News = ({ blok }) => {
    //return <div {...storyblokEditable(blok)}>{blok.site_name}</div>;
    return <div>News</div>;
};

export default News;
